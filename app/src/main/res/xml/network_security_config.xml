<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Local IoT prototypes frequently expose HTTP. Replace with device-scoped
         TLS or a vendor SDK before production use. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
